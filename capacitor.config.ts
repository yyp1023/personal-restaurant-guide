import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'personal-restaurant-guide',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
